# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Virtual envs
.venv/
venv/

# Secrets
.env

# Arena runtime state & generated artifacts (repo keeps source/docs only;
# these are produced by `python -m arena run` and tests)
reports/orchestrator_state.json
reports/elo_state*.json
reports/cache/
reports/fused/
reports/improved/
reports/report_*.md
reports/events/
reports/_pytest_*.log
reports/matches.jsonl
reports/elo_state_minimal.json

# 运行时生成的任务(LLM 动态生成,可复现)
tasks/auto/

# User-imported skills are personal; keep the dir but ignore its contents
import_skills/*
!import_skills/.gitkeep

# Editor / tooling
.opencode/
.claude/
.codegraph/
.kunsdd/
.mavis/
